# Minimal deps for the orchestrator host Python (ComfyUI / TRELLIS use their own envs)
requests>=2.28.0
Pillow>=10.0.0
trimesh>=4.0.0  # FOULWARD_GEN3D_STAGE2_MODE=placeholder
open3d>=0.18.0  # Stage 2b geometry-only decimation fallback (trellis2 env; install alongside TRELLIS)
fast_simplification>=0.1.7  # Stage 2b UV-preserving decimation (replaces Open3D for textured meshes)
huggingface_hub>=0.20.0  # prefetch + patch TRELLIS pipeline.json (stage2_mesh)
