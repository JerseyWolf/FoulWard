You are a careful file-system and git configuration agent. Move ONLY the two
paid MCP server folders out of this git repo to a safe location one level above
the repo root, then update every file that references their old paths.

DO NOT touch: sequential-thinking, filesystem-workspace, github, foulward-rag.
Those stay in the repo. Only move these two:
  - godot-mcp-pro
  - gdai-mcp-godot

STEP 1 — Find exact current locations.
Run: find . -maxdepth 6 -type d \( -name "godot-mcp-pro" -o -name "gdai-mcp-godot" \)
Confirm the paths before touching anything.

STEP 2 — Create destination folder above the repo root.
Run: mkdir -p ../foulward-mcp-servers

STEP 3 — Move only the two paid folders.
Run:
  mv <found-path-to-godot-mcp-pro>  ../foulward-mcp-servers/godot-mcp-pro
  mv <found-path-to-gdai-mcp-godot> ../foulward-mcp-servers/gdai-mcp-godot
Confirm with: ls ../foulward-mcp-servers/

STEP 4 — Update .cursormcp.json (or mcp.json).
Run: find . -maxdepth 3 \( -name ".cursormcp.json" -o -name "mcp.json" \) | grep -v node_modules
For the godot-mcp-pro and gdai-mcp-godot entries ONLY, change any "cwd",
"command", or "args" value that pointed inside the repo so it now points to
../foulward-mcp-servers/<server-name>. Leave all other server entries alone.

STEP 5 — Update .cursor/skills/mcp-workflow/SKILL.md.
Run: find . -path "*mcp-workflow*SKILL.md"
Update the path references for godot-mcp-pro and gdai-mcp-godot to
../foulward-mcp-servers/. Add a note next to those two rows:
"Source lives OUTSIDE the repo at ../foulward-mcp-servers/ (paid — not distributed)."

STEP 6 — Update FOUL_WARD_MASTER_DOC.md Section 1.1.
In the MCP server table, add to the godot-mcp-pro and gdai-mcp-godot rows:
"server source at ../foulward-mcp-servers/<name> (outside repo)".

STEP 7 — Update docs/AGENTS.md.
Run: find . -name "AGENTS.md" -not -path "*node_modules*"
In each file found, replace in-repo path references for the two paid servers
with ../foulward-mcp-servers/<name>.

STEP 8 — Update .cursor/rules/mcp-godot-workflow.mdc if it exists.
Run: find . -name "mcp-godot-workflow.mdc"
If found, replace old in-repo paths for the two paid servers.

STEP 9 — Scan for any remaining stale references.
Run: grep -r "godot-mcp-pro\|gdai-mcp-godot" . \
  --include="*.md" --include="*.json" --include="*.mdc" --include="*.cfg" \
  --exclude-dir=".git" -l
Fix any file that still has the old in-repo path.

STEP 10 — Add .gitignore safety net (append if not already present).
  # Paid MCP server code — lives OUTSIDE repo at ../foulward-mcp-servers/
  /tools/mcp-support/godot-mcp-pro/
  /tools/mcp-support/gdai-mcp-godot/

STEP 11 — Commit the reference changes.
Run:
  git add -A
  git commit -m "chore: move paid MCP servers above repo root, update all path references"

DO NOT push yet. The history purge runs next.