You are a documentation synchronisation agent. The two paid MCP servers
(godot-mcp-pro and gdai-mcp-godot) have been moved to ../foulward-mcp-servers/.
Your job is to make sure every documentation file in this repo accurately
reflects that change and is fully up to date. Do not invent new content —
only update existing references and add the factual notes listed below.

STEP 1 — Audit all docs for stale content before changing anything.
Run: grep -r "godot-mcp-pro\|gdai-mcp-godot\|tools/mcp-support" . \
  --include="*.md" --include="*.mdc" --include="*.txt" \
  --exclude-dir=".git" -n
List every file and line number returned. This is your work list.

STEP 2 — Update FOUL_WARD_MASTER_DOC.md.
  a. In Section 1.1 MCP server table, update the godot-mcp-pro row so the path
     column reads: "../foulward-mcp-servers/godot-mcp-pro (OUTSIDE REPO — paid content)".
  b. Do the same for gdai-mcp-godot.
  c. In the changelog table at the top, add a new row:
     Date: today's date | Author: Prompt 4 automation | Summary: Moved paid MCP
     server sources above repo root; updated all doc references.
  d. In Section 34 Related Documents, if .cursormcp.json is listed, add a note:
     "godot-mcp-pro and gdai-mcp-godot server source now at ../foulward-mcp-servers/".
  e. Do not change any other content in the master doc.

STEP 3 — Update docs/AGENTS.md (root-level).
  a. Find the MCP server list. Change the path for godot-mcp-pro and
     gdai-mcp-godot to: ../foulward-mcp-servers/<name>
  b. Add a one-line callout directly above the server table:
     "NOTE: godot-mcp-pro and gdai-mcp-godot live OUTSIDE the repo at
     ../foulward-mcp-servers/ and are not distributed with the public codebase."
  c. Do not change anything else in AGENTS.md.

STEP 4 — Update .cursor/skills/mcp-workflow/SKILL.md.
  a. Find the server table (6 servers). For godot-mcp-pro and gdai-mcp-godot rows:
     - Update any path, cwd, or location reference to ../foulward-mcp-servers/<name>
     - Add a note column or inline note: "(paid — outside repo)"
  b. Add or update the Prerequisites section to include:
     "godot-mcp-pro and gdai-mcp-godot are not in this repo.
     They live at ../foulward-mcp-servers/ relative to the repo root.
     Do not attempt to find or install them from within the repo."
  c. Do not change the server usage instructions, port numbers, or anything else.

STEP 5 — Update .cursor/rules/mcp-godot-workflow.mdc if it exists.
Run: find . -name "mcp-godot-workflow.mdc"
If found:
  a. Replace any in-repo path for the two paid servers with ../foulward-mcp-servers/<name>.
  b. Add a comment block at the top of the file:
     <!-- godot-mcp-pro and gdai-mcp-godot live at ../foulward-mcp-servers/
          They are paid tools not included in the public repository. -->

STEP 6 — Update docs/INDEXSHORT.md if it exists and lists MCP paths.
Run: find . -name "INDEXSHORT.md"
If found, search for godot-mcp or gdai-mcp entries and update their paths.

STEP 7 — Update docs/INDEXFULL.md if it exists and lists MCP paths.
Run: find . -name "INDEXFULL.md"
If found, search for godot-mcp or gdai-mcp entries and update their paths.

STEP 8 — Final grep to confirm no stale references remain.
Run: grep -r "tools/mcp-support/godot-mcp-pro\|tools/mcp-support/gdai-mcp-godot" . \
  --include="*.md" --include="*.mdc" --include="*.txt" \
  --exclude-dir=".git"
PASS: no output. If anything is printed, fix it before committing.

STEP 9 — Commit all doc changes.
Run:
  git add -A
  git commit -m "docs: update all references to reflect paid MCP servers moved above repo root"

STEP 10 — Print completion summary.
List every file that was modified and the specific change made in each one.
Then print: "Doc update complete. All references to godot-mcp-pro and
gdai-mcp-godot now point to ../foulward-mcp-servers/."