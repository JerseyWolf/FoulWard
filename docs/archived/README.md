# Archived material

This directory is **not** the live source of truth. It exists so historical session logs remain in the repo (and in `git log`) without cluttering `docs/`.

## What lives here now

| Path | Role |
|------|------|
| **`prompts/`** | Historical `PROMPT_N_IMPLEMENTATION.md` session logs (rolling window: the 10 newest are under `docs/`). See **`prompts/README.md`**. |

Older one-off audits, dumps, and pre-batch planning files that used to live under `docs/archived/` were **removed** in the 2026-04-20 cleanup (they remain reachable via Git history if needed).

## Current documentation entry points

- Repo root **`README.md`** — GitHub landing page.
- **`HOW_IT_WORKS.md`**, **`INTERVIEW_CHEATSHEET.md`** — engineering narrative for interviews.
- **`docs/README.md`**, **`docs/INDEX_SHORT.md`**, **`docs/FOUL_WARD_MASTER_DOC.md`**, repo-root **`AGENTS.md`**.
